DSEG	SEGMENT
		NUM		DB 1000 DUP(?)
DSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG
START:	MOV		AX,DSEG
		MOV		DS,AX

		MOV 	AX,1018H
		MOV 	SI,030AH
		MOV		[SI],AX
		ADD		AL,30H
		MOV 	DX,3FFH
		ADD		AX,DX
		MOV 	DS:WORD PTR[20H],1000H
		ADD		[SI],AX
		PUSH	AX
		POP		BX

		MOV 	AH,4CH
		INT		21H
CSEG	ENDS
		END		START