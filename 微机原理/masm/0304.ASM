DSEG	SEGMENT
		NUM		DB 1000 DUP(?)
DSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG
START:	MOV		AX,DSEG
		MOV		DS,AX

		MOV 	DS:WORD PTR[10H],80H
        MOV     BL,4
        MOV     AX,DS:[10H]
        DIV     BL
        
		MOV 	AH,4CH
		INT		21H
CSEG	ENDS
		END		START